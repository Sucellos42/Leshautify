//on supprime tous les enfant de nav-right left




/*const nav_right = document.querySelector('.navbar-right')


//On selection tous les liens de la nav de droite pour que quand on clique dessus on récupère l'id pour rediriger ensuite

const select = document.querySelectorAll('.navigation__nav-list-item')
console.log(select)
/!*while(nav_right.firstChild) {
    nav_right.removeChild(nav_right.firstChild)
}*!/
select.forEach(e => {
    console.log(e)
    e.addEventListener('click', item => {
        console.log(item.target.id)
    })
})*/






//on crée une div qui contiendra une grid de chaque morceaux

//si la largeur est plus petit que tant on cache les artistes et les albums sinon on les remet

